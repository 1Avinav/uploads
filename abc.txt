CREATE SCHEMA IF NOT EXISTS x;

CREATE TABLE IF NOT EXISTS x.users (
    user_id SERIAL PRIMARY KEY,
    role VARCHAR(50) NOT NULL
);

INSERT INTO x.users (user_id, role) VALUES
    (1, 'ADMIN'),
    (2, 'USER'),
    (3, 'MODERATOR')
ON CONFLICT DO NOTHING;  -- Avoid duplicate entries if re-running
